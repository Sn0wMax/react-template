// Import action
//import { SAMPLE_ACTION } from '../actions/index';

// Set Initial State
//const INITIAL_STATE = { key : object };

// Switch for type
/*
export default function(state = INITIAL_STATE, action) {
  switch(action.type) {
  case SAMPLE_ACTION:
    // return combined state
    return { ...state, key: action.payload.data };
  default:
    // default always return unmodified state
    return state;
  }
}
*/