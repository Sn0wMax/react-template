//import React, { Component } from 'react';
//import { connect } from 'react-redux';

// Import actions
//import { sampleAction } from '../actions/index';

/*
class SampleComponent extends Component {
  // Setup
  componentWillMount() {
    
  }

  // Render
  render() {
    return (
      <div>
      </div>
    );
  }
}
*/

// State to Props function
// Adds objects from state to props behind used keys
/*
function mapStateToProps(state) {
  return { key: state.object };
}
*/

// Connect state and actions
// Set first param to null if not needed
//export default connect(mapStateToProps, { sampleAction })(SampleComponent);
